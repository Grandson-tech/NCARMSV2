// Copy this file to runtime-config.js and provide development values locally.
// Production should inject the same global object before application modules load.
globalThis.NCARMS_SUPABASE_CONFIG = Object.freeze({
  url: '',
  anonKey: '',
});
